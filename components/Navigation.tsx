const Navigation = () => {
    return (
        <nav className={`flex flex-col justify-between items-center h-full w-full bg-black overflow-hidden mt-10`}>
            <h1 className={`text-orange`}>SKOPJE PAWS</h1>
            <h2>The first personal dog walking services. Not just professionals, but animal lovers too.</h2>
        </nav>
    );
};

export default Navigation;
